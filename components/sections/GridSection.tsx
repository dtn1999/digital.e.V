import React from "react";
import cn from "classnames";
interface Props {}
const GridSection: React.FC<Props> = React.memo(({}) => {
  return <div className={cn("")}> GridSection </div>;
});
export default GridSection;
